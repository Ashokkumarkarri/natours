now we create a new folder: controller.
in that folder will cut and paste the code related to route handelers.

In future in our project we will create controller and we will use this code.
thats why we are calling the route handelers a controllers.

----------------------
tourControllers.js: 
in this file we had pasted all the code from tourRoutes.js;
all routehandelers are moved here.

now we need to export the functions from this file.
since there are bunch of function need to be exported we will use:
we goona add exports. inthe place of const to the varible to be exported.
example:
exports.deleteTour = (req, res) => {
  if (req.params.id * 1 > tours.length) {
    return res.status(404).json({
      status: 'fail',
      message: 'Invalid ID',
    });
  }
  res.status(204).json({
    status: 'Success',
    data: null,
  });
};
------------------------------
tourRoute:
we had imported the files from tourControllers

old code:

const express = require('express');
const router = express.Router();
router.route('/').get(getAllTours).post(createTour);

router.route('/:id').get(getTour).patch(updateTour).delete(deleteTour);
module.exports = router;

new code:
since we hade imported the functions from tourControllers, all the methods which are there in that file. we can accesss in this file too.

const express = require('express');
const tourController = require('./../controllers/tourController');

const router = express.Router();
router
  .route('/')
  .get(tourController.getAllTours)
  .post(tourController.createTour);

router
  .route('/:id')
  .get(tourController.getTour)
  .patch(tourController.updateTour)
  .delete(tourController.deleteTour);

module.exports = router;

---------------------
userRoute:
similarly also changed userRoute file
----------------------
