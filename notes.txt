here we had implemented params middleware.
router.param('id', (req, res, next, val) => {
  console.log(`Tour id is;${val}`);
  next();
});


---------------------------------------
and I had also replaced the code tourRoutes:
  if (req.params.id * 1 > tours.length) {
    return res.status(404).json({
      status: 'fail',
      message: 'Invalid ID',
    });
  }
  --------------------------------------


  in expresss we best work with middleware pipelane.

  