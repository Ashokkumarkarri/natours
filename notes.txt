To set up ESLint and Prettier to highlight errors and ensure code quality, follow these steps:

1. Install the ESLint extension in VS Code:.
2. Install necessary dev dependencies:
Run the following command to install ESLint, Prettier, and related packages:
npm i eslint prettier eslint-config-prettier eslint-plugin-prettier --save-dev


----------
npm i eslint prettier eslint-config-prettier eslint-plugin-prettier eslint-config-airbnb eslint-plugin-node eslint-plugin-import eslint-plugin-jsx-a11y eslint-plugin-react --save-dev

------------
after installing we need config file for both eslint and prettier.
we had created prettier.

-----------

create a file called : eslintrc.json
in that file wrtie these rules 

{
  "extends": ["airbnb", "prettier", "plugin:node/recommended"],
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": "error",
    "spaced-comment": "off",
    "no-console": "warn",
    "consistent-return": "off",
    "func-names": "off",
    "object-shorthand": "off",
    "no-process-exit": "off",
    "no-param-reassign": "off",
    "no-return-await": "off",
    "no-underscore-dangle": "off",
    "class-methods-use-this": "off",
    "prefer-destructuring": ["error", { "object": true, "array": false }],
    "no-unused-vars": ["error", { "argsIgnorePattern": "req|res|next|val" }]
  }
}
------------------

